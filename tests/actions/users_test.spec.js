const chai = require('chai');

const { expect } = chai;
const sinon = require('sinon');
const UsersAction = require('../../actions/users');

describe('Users actions tests', () => {
  it('Get all function', async () => {
    //
  });
});
